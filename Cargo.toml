[package]
name = "rx-rust"
version = "0.3.0"
edition = "2024"
rust-version = "1.85.0"
license-file = "LICENSE"
repository = "https://github.com/623637646/rx-rust"
description = "Reactive Programming in Rust inspired by ReactiveX https://reactivex.io/"

[dependencies]
educe = "0" # Using it to fix the issue: https://github.com/rust-lang/rust/issues/26925  Refer to: https://stackoverflow.com/a/79571732/9315497
slotmap = "1"
cfg-if = "1"
futures = { version = "0", optional = true } # It's optional even though most rust runtimes depend on it because users may implement their own runtimes without depending on futures.
tokio = { version = "1", optional = true }
async-std = { version = "1", optional = true }
smol = { version = "2", optional = true }
async-io = { version = "2", optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
async-io = { version = "2" }
paste = "1"

[features]
default = []

# no scheduler
single-threaded = [] # single-threaded mode without scheduler.

# scheduler
local-pool-scheduler = ["single-threaded", "futures", "dep:async-io"]
thread-pool-scheduler = ["futures/thread-pool", "dep:async-io"]
tokio-scheduler = ["futures", "dep:tokio", "tokio/rt", "tokio/time"]
async-std-scheduler = ["futures", "dep:async-std"]
smol-scheduler = ["futures", "dep:smol"]